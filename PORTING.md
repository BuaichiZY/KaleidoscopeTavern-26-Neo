# Minecraft 26.2 移植说明

本工程以功能完整的 Minecraft 26.1.2 移植版为基线，继续移植到：

- Minecraft 26.2
- NeoForge 26.2.0.68 及以上
- Java 25
- 本地 Gradle 9.6.1（项目 Wrapper 保留 9.4.1）

在 Windows 下使用项目自带的 Gradle Wrapper 构建，例如：

```text
gradlew.bat compileJava
gradlew.bat build
gradlew.bat runData
```

构建产物位于 `build/libs/`；本地参考文件和工作目录不会进入源码仓库或最终模组。

## 已完成验证

- `runData`：通过，430 个当前格式的数据文件成功生成，无资源被误删。
- `build`：通过，主 JAR 与源码 JAR 均已生成。
- `runGameTestServer`：通过，专用服务器完成数据包加载，1 项必需测试通过。
- 主类字节码版本为 69（Java 25）。

当前 26.2 版本：`v1.0.0`

最终主 JAR：`build/libs/kaleidoscopetavern-26.2-v1.0.0.jar`

源码 JAR：`build/libs/kaleidoscopetavern-26.2-v1.0.0-source.jar`

## 26.2 迁移内容

- 将 Minecraft 与 NeoForge 依赖升级到 26.2 / 26.2.0.68，并迁移染料、羊毛、标签数据生成、成就触发器、即时效果、文本格式和客户端界面等 26.2 API。
- 重新接通 26.2 的第一人称手持物品提交入口，保留原版雪克杯摇酒变换及既有 Punchy、Player Animation Library 兼容逻辑。
- 使用 26.2 数据生成器更新 430 个可再生成资源，同时保留完整的手工模型、方块状态、语言、配方及兼容资源。
- 开发客户端已成功加载 Minecraft、NeoForge、本模组、JEI 与 Jade，全部客户端 Mixin、Jade 插件和资源包均正常载入。
- `compileJava`、`runData` 与 `runGameTestServer` 均通过；专用游戏测试共 1 项，结果为 1/1 通过。

## 26.1.2 基线修复历史

### v1.1.24

- 根据整合包日志恢复 18 个未纳入 26.1.2 数据生成器的有序合成配方，包括雪克杯、酒杯、酒架、垂灯、窖藏酒柜和 8 种香薰；全部配料已改为 26.1.2 的字符串 Ingredient 格式，并将旧版 `minecraft:chain` 更新为 `minecraft:iron_chain`，对应配方解锁进度也已同步重新生成。
- 移除指向不存在文件的 Mixin refmap 配置；保留全部客户端 Mixin，并消除生产环境启动时的无效 refmap 警告。

### v1.1.22

- 以用户提供的 `kaleidoscopetavern-1.1.2-neoforge mc26.1.2.jar` 为准恢复座椅实现：沙发和高脚凳重新使用 Minecraft 26.1.2 原生乘客附着点，仅下移 `1/16` 格；移除后来叠加的座椅腿部强制覆盖，让原版与 Punchy 直接处理乘客坐姿。保留高脚凳乘客追踪和雪克杯动画兼容。

### v1.1.21

- 按最新整合包日志确认 Player Animation Library 会在原版姿态后覆盖雪克杯手臂；改为直接读取最终 `HumanoidRenderState` 中的使用物品，并在 PAL 回调之后重建 1.20.1 摇酒角度。对 Punchy 额外保存并在 `ModelPart` 末端应用的显式矩阵增加渲染时保护，避免其再次覆盖摇酒动作。
- 为沙发和高脚凳增加独立的 Tavern 座位渲染状态；最终玩家模型会重建原版弯腿坐姿，并在 Punchy/PAL 实际提交模型部件时清除腿部的附加动画矩阵，不影响船、马等其他载具的动画。

### v1.1.19

- 第三人称摇酒不再依赖可能被 Punchy/Player Animation Library 改写的原版手臂姿势枚举；在玩家渲染状态中保留实际使用雪克杯的手，并在最终玩家与护甲模型骨骼阶段恢复 1.20.1 摇酒动作。
- 按 26.1.2 新乘客连接点公式重新换算旧版 `-0.25` 座位偏移，消除额外的 `0.6` 格下沉；同时在 Player Animation Library 的最终腿部骨骼阶段恢复本模组沙发和高脚凳的原版坐姿。

### v1.1.17

- 增加 Punchy 专用兼容：让雪克杯自动使用 Punchy 官方预留的物品黑名单绕过路径，恢复 NeoForge 原生持物渲染、使用状态和第三人称动画处理；不会修改用户的 Punchy 配置，也不会影响其他物品的 Punchy 动画。
- 恢复 1.20.1 原版座位锚点高度并明确启用乘客坐姿，修复沙发和高脚凳上玩家呈站立状态的问题；同时保留高脚凳座位实体 ID 并延迟解析，恢复凳面随乘客朝向转动的动作。

### v1.1.15

- 根据实际整合包录像与日志，改为在 `AvatarRenderer` 最终渲染状态中直接标记持杯手，并在 `ItemInHandRenderer` 的 NeoForge 扩展调用点直接执行 1.20.1 变换；绕过 Vivecraft、Player Animation Library、EMF 等并存时未命中的间接物品姿势通道。

### v1.1.14

- 修正 v1.1.13 中雪克杯自定义 `ArmPose` 提前返回、导致 Cookery 拉面团同款 `BOW` 使用动画从未进入玩家姿势计算的问题；现在先走原生 `BOW` 通道，再在其实际执行点将雪克杯替换为 1.20.1 原版摇酒摆臂，使 Player Animation Library 等动画模组取得正确的基础骨骼动作。

### v1.1.13

- 参照 Kaleidoscope Cookery 26.2 的拉面团实现，为雪克杯补充原生持续使用动画信号；同时在 Player Animation Library 实际更新玩家骨骼的位置读取实时使用状态并应用原版摇酒角度，覆盖其第一、第三人称动画接管路径。

### v1.1.12

- 根据实际整合包日志确认 `Player Animation Library` 会在 NeoForge 物品姿势接口之后覆盖玩家骨骼；现在于其最终动画阶段之后重新应用 1.20.1 原版雪克杯角度，使第三人称与该动画库接管的第一人称全身模型都能得到摇酒动作。
- JEI 雪克杯输入槽恢复最初的原生 `Ingredient` 渲染与轮换，不再对酒瓶标签的白色 `#` 做额外处理。

### v1.1.10

- 按 1.20.1 原实现重新接通 NeoForge 26.1.2 的客户端物品扩展：第一人称沿用原始平移、俯仰和正弦上下摇动，独立提交的玩家手臂在摇酒时隐藏；第三人称通过扩展 `ArmPose` 读取 `HumanoidRenderState.ticksUsingItem(arm)`，恢复原版大幅摆臂动作，并移除上一版无效的模型 Mixin。
- JEI 输入槽改为只管理配料轮换、焦点与悬停，酒瓶图标在配方末层直接从本模组原始物品贴图绘制，从渲染链上隔离标签 `#` 标识。

### v1.1.8

- 在玩家渲染状态抽取时记录实际使用雪克杯的手臂，并在 `PlayerModel` 的最终动画阶段直接恢复原版摆动角度，避免中间姿势或物品状态映射失效。
- JEI 雪克杯原料槽改用无物品装饰层的酒瓶渲染器，保留图标、槽位和悬停说明，彻底移除白色 `#`。

### v1.1.6

- 在玩家模型完成 vanilla 动画后重新应用原版雪克杯手臂角度，避免 26.1 渲染提交阶段覆盖第三人称调酒动作。
- 将 JEI 的酒类标签原料展开为实际物品列表循环显示，保留槽位边框并移除白色 `#` 标签角标。

### v1.1.4

- 恢复含水方块的专用龙头行为，龙头会按 `WATERLOGGED` 方块状态正确取水。
- 按视频 1 的旧版动作修正 26.1 玩家渲染状态中的雪克杯手臂姿势，避免第三人称动作回退为普通持物姿势。
- 恢复酒类悬停面板中的颜色名称及对应文字颜色。
- 恢复 JEI 雪克杯原料槽和产物槽背景，并让原料标签由 JEI 原生解析，修复井号状残影及颜色格边界。

### v1.1.0

- 重大 bug 修复：从方块图集读取调酒原料颜色菱形图标，恢复正确的颜色 UI。
- 恢复 JEI 雪克杯分类、全部鸡尾酒配方及雪克杯工作站注册。
- 通过 NeoForge 客户端物品扩展和枚举扩展恢复第一人称雪克杯变换与第三人称手臂调酒动作。

### v1.0.4

- 为全部方块物品启用 26.1.2 的 `block.*` 描述前缀，修复玻璃瓶、酒类、鸡尾酒和装饰方块显示英文的问题。
- 修正展板、挂画及 JEI 酒桶/果盆分类的翻译键。

### v1.0.3

- 补齐全部 157 个注册物品的 26.1.2 物品定义；同步修复 15 种酒具的 16 段旋转模型、调酒壶显示上下文模型和垂灯锁链纹理。
- 果盆中的果汁统一经容器填充交互提取，避免装满果汁后使用桶时额外取出一个葡萄。
- 从 1.2.0 原版参考包恢复完整的 `en_us`、`ja_jp`、`ru_ru`、`zh_cn` 语言文件。
- 开发客户端资源重载检查：本模组缺失模型、状态解析失败及缺失纹理均为 0。

## 可选兼容说明

核心内容以及 JEI 的调酒壶配方显示已移植。EMI、REI 的调酒壶专用配方页面仍依赖旧版查看器 API，相关源码保留作后续参考，但不会编入当前发布包。
